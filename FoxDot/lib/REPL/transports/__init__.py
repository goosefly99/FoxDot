"""
REPL Transport implementations.

Available transports:
    stdin_transport.StdinTransport     — line-delimited JSON over stdin/stdout
    websocket_transport.WebSocketTransport — JSON over WebSocket
"""
